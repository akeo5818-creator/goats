{
  "name": "bloxy-moderation-bot",
  "version": "2.4.0",
  "private": true,
  "type": "module",
  "description": "Standalone Discord moderation, security, analytics, tournaments, chat drops, DM polls and a Customer-only read-only FAQ system with duplicate-panel recovery and V2 components.",
  "main": "src/index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.js",
    "check": "node --check src/index.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "discord.js": "^14.27.0"
  }
}
